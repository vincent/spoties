
volumes:
  spoties_data:

services:

  spoties:
    build: .
    image: spoties
    hostname: backend
    restart: unless-stopped
    volumes:
      - spoties_data:/srv/spoties/data
    ports:
      - "80:1080"
      - "443:1443"
